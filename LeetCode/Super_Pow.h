#pragma once

#include <vector>

using namespace std;

class Super_Pow{

public:
	int superPow(int a, vector<int>& b);

	char* exponent(char* a, char* b);

	char* int_to_char(int a);

	char* char_mul(char* a, char* b);

	char* char_mod(char* a, char* b);

	char* char_div(char* a, char* b);
};
